let percentage = 0; const percentageElement = document.getElementById('percentage'); const messageElement = document.getElementById('message'); const increaseButton = document.getElementById('increase'); const decreaseButton = document.getElementById('decrease'); increaseButton.addEventListener('click', () => { if (percentage < 100) { percentage += 10; percentageElement.textContent = `${percentage}%`; if (percentage === 10) { messageElement.style.display = 'block'; } messageElement.style.opacity = percentage / 100; } }); decreaseButton.addEventListener('click', () => { if (percentage > 0) { percentage -= 10; percentageElement.textContent = `${percentage}%`; if (percentage === 0) { messageElement.style.display = 'none'; } messageElement.style.opacity = percentage / 100; }})


